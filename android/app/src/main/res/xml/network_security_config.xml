<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow plain HTTP only to the dev backend that runs on the host machine.
         10.0.2.2 is the Android emulator's alias for the host's loopback.
         Add your LAN IP here if you test on a physical device on the same Wi-Fi.
         Production / release builds should rely on HTTPS to a real domain and
         this file can be removed. -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
